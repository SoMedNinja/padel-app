# Required for Supabase connection (all environments)
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Required for Push Notifications (generate with `npx web-push generate-vapid-keys`)
# The public key goes here. The private key should be used in your backend (e.g. Edge Functions).
VITE_WEB_PUSH_PUBLIC_KEY=your_vapid_public_key
