# Release notes

## Unreleased
- Authentication: “Remember me” now depends on refresh tokens (the long-lived session ticket) instead of a 30‑day JWT, so short-lived access tokens can expire without logging users out.

